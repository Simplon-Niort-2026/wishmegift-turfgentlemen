info:
  name: create user
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost:8080/user/register
  body:
    type: json
    data: |-
      {
        "username" : "amelie",
        "password": "12345678Am!$",
        "email": "amelie@gmail.fr"
        
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
