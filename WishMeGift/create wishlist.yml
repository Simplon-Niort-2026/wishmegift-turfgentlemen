info:
  name: create wishlist
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/wishlist
  body:
    type: json
    data: |-
      {
        "listName": "la liste de moi même",
        "theme": "NOEL",
        "description": "c'est pour mon noël",
        "eventDate": "2026-12-25"
       
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
