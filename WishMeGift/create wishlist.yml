info:
  name: create wishlist
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/wishlist/eef0561d-de7b-4fb6-b6af-e2c87e272e36
  body:
    type: json
    data: |-
      {
        "listName": "la  deuxième liste",
        "theme": "NOEL",
        "description": "c'est pour mon noël",
        "eventDate": "2026-12-25"
       
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
